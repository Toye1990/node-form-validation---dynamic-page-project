<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/assets/style.css">
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
</head>
<body>
    <div class="container-1">
       {{>header}}

<section class="profile-cover">
  <div class="all-col">
    <div class="profile-image">
        <img src={{studentpro.image}} alt="student profile">
    </div>
    <div class="profile-info">
        <div class="required">
            <div>Name</div>
            <div>Occupation</div>
            <div>Gender</div>
            <div>City</div>
            <div>State</div>
        </div>
        <div class="info-res">
            <div>{{studentpro.name}}</div>
            <div>{{studentpro.occupation}}</div>
            <div>{{studentpro.gender}}</div>
            <div>{{studentpro.city}}</div>
            <div>{{studentpro.state}}</div>
        </div>
    </div>
</div>
    <div class="down-text">
        <h2>BIO</h2>
        <p>{{studentpro.description}}</p>
 
</section>

<div class="container-fluid">
<div class="mt-3 d-flex align-items-center justify-content-center gap-2">
    <a href="" class="btn btn-primary">previous</a> 
    {{#each checkactivenow}}
    <a href="/students/{{id}}" class="btn {{#if active}}btn-primary{{else}}btn-outline-primary{{/if}}">{{studentNum}}</a>
    {{/each}}
    <a href="" class="btn btn-primary">next</a>
</div>
</div>


{{>footer}}

</div>
</body>
</html>